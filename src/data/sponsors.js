/**
 * Default sponsors (optional). Used until you save a list in the Dashboard;
 * then `localStorage` overrides this file. Logo paths are usually under `public/sponsors/`
 * (e.g. `logo: '/sponsors/acme.svg'`).
 *
 * @typedef {{ name: string, url: string, logo?: string | null, logoDataUrl?: string, description?: string }} Sponsor
 * @type {Sponsor[]}
 */
export const sponsors = []
